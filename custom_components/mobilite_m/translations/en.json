{
  "config": {
    "step": {
      "user": {
        "title": "Search for a stop",
        "description": "Enter the name of a stop or stop cluster (minimum 3 characters).",
        "data": {
          "stop_query": "Stop name"
        }
      },
      "select_cluster": {
        "title": "Choose stop cluster",
        "description": "Select the desired stop cluster from the results.",
        "data": {
          "cluster": "Stop cluster"
        }
      },
      "select_modes": {
        "title": "Filter by transport mode",
        "description": "Select the transport modes to monitor (leave empty for all modes).",
        "data": {
          "modes": "Modes (optional)"
        }
      },
      "select_lines": {
        "title": "Filter by line",
        "description": "Select the lines to monitor (leave empty for all lines).",
        "data": {
          "lines": "Lines (optional)"
        }
      },
      "select_destinations": {
        "title": "Filter by destination",
        "description": "Select the destinations to monitor (leave empty for all destinations).",
        "data": {
          "destinations": "Destinations (optional)"
        }
      }
    },
    "error": {
      "query_too_short": "Search must be at least 3 characters.",
      "no_results": "No stops found. Try a different search term.",
      "cannot_connect": "Unable to connect to the Mobilités-M API."
    },
    "abort": {
      "already_configured": "This stop cluster (with this filter) is already configured."
    }
  }
}
